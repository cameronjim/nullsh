// Contracts-commit stub; the run agent replaces this file.

#include "run.h"

NshError run_interactive(Shell *sh) {
    (void)sh;
    return NSH_ERR_INVALID;
}

NshError run_stream(Shell *sh, FILE *fp) {
    (void)sh;
    (void)fp;
    return NSH_ERR_INVALID;
}
